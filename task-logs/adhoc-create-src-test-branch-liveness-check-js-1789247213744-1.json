{
  "id": "adhoc-create-src-test-branch-liveness-check-js-1789247213744-1",
  "title": "Create src/test-branch-liveness-check.js",
  "domain": "adhoc",
  "source": "manual",
  "createdAt": "2026-09-13T01:40:38.272Z",
  "history": [
    {
      "stage": "created",
      "at": "2026-09-13T01:40:38.273Z",
      "detail": "manual"
    },
    {
      "stage": "draft-started",
      "at": "2026-09-13T01:40:39.296Z"
    },
    {
      "stage": "orient-done",
      "at": "2026-09-13T01:44:14.194Z",
      "detail": "8 turn(s)"
    },
    {
      "stage": "plan-done",
      "at": "2026-09-13T01:48:47.665Z",
      "detail": "3 attempt(s), 9812 chars"
    },
    {
      "stage": "implement-started",
      "at": "2026-09-13T01:48:56.256Z",
      "detail": "adhoc: local-agentic-write (multi-turn edit/write/run_bash in a worktree -- can take many minutes)"
    },
    {
      "stage": "implement-done",
      "at": "2026-09-13T01:52:47.542Z",
      "detail": "agentic, 8445 chars, resolution=implemented"
    },
    {
      "stage": "draft-done",
      "at": "2026-09-13T01:52:47.542Z",
      "detail": "resolution=implemented, qwen3.8:27b-q4_K_M"
    },
    {
      "stage": "needs-review",
      "at": "2026-09-13T01:52:47.562Z"
    },
    {
      "stage": "draft-attempt",
      "at": "2026-09-13T01:52:47.563Z",
      "detail": "attempt 1: succeeded, tiers: local-agentic-write=implemented"
    },
    {
      "stage": "review-started",
      "at": "2026-09-13T01:53:26.001Z"
    },
    {
      "stage": "approved",
      "at": "2026-09-13T01:53:50.285Z",
      "detail": "votes: 2/3 real"
    }
  ],
  "promptContext": {
    "rawText": "Create src/test-branch-liveness-check.js using node:test + node:assert/strict, creating a temp git repo (fs.mkdtempSync + git init/commit/branch via execFileSync) to cover: existing branch -> exit 0/alive true; deleted branch (`git branch -D`) -> exit 1/alive false with reason mentioning gone/not on main; missing repo path -> exit 2 with error true; and the CLI contract (single JSON object with branch/alive/reason keys) by spawning `node src/branch-liveness-check.js`. `node src/test-branch-liveness-check.js` must exit 0 — run it to confirm before finishing.",
    "decomposedFrom": "adhoc-brain-dump-bd-1788757832868-adhoc-auth-task-work-lost-to-a-dead-bran-1788858215751"
  },
  "draftAttempts": [
    {
      "at": "2026-09-13T01:40:39.295Z",
      "attemptNo": 1,
      "source": "manual",
      "localRejectCount": 0,
      "tiers": [
        {
          "tier": "local-agentic-write",
          "resolution": "implemented",
          "blocked": false,
          "responseChars": 3075,
          "response": "All checks pass. The `git add`/commit failed only because the sandbox's `.git/objects` directory is on a read-only filesystem (env limitation, exit 128 \"Read-only file system\") — the file itself is written on disk and all acceptance-criteria checks that matter (file exists, tests run, exit 0) pass. The commit is normally handled by the pipeline harness.\n\nSummary: I created `src/test-branch-liveness-check.js` targeting the real, on-disk `src/branch-liveness-check.js` (the sibling's \"merged\" file was genuinely present — the prior investigation's ENOENT was stale). Two corrections to the pre-plan: the CLI takes **flags** `--branch <name> --repo <path>`, not positional args; and the deleted-branch test had to use a branch name absent from all commit messages, because the CLI's tip-discovery falls back to `git log --all --grep=<branch>`, which would otherwise resurrect the deleted branch as \"work already on main\" and flip it to alive.\n\nAcceptance:\n1. `src/test-branch-liveness-check.js` exists and imports `node:test` + `node:assert/strict` — `grep -n \"node:test|node:assert/strict\"` — PASS (lines 14-15: `require('node:test')`, `require('node:assert/strict')`)\n2. `node src/test-branch-liveness-check.js` exits 0 with ≥4 passing tests — ran it — PASS (`# pass 4 / # fail 0`, `EXIT_CODE=0`, all 4 named cases ok)\n3. Each test creates its own temp git repo via `fs.mkdtempSync` + `execFileSync('git', …)` and cleans up — `grep -cn \"mkdtempSync|rmSync\"` = 5, plus 6 `execFileSync('git'` sites — PASS (per-test `createTempRepo()` + `t.after(() => fs.rmSync(dir, {recursive:true, force:true}))`)\n4. Deleted-branch test asserts `res.status === 1`, `out.alive === false`, reason mentions \"gone\"/\"not on main\" — `grep -n` lines 75/79/82 — PASS (test ran green; actual CLI reason \"branch gone, not on main: work would be lost\" satisfies both substrings)\n5. Missing-repo test asserts `res.status === 2` and `out.error === true` — `grep -n` lines 92/95 — PASS (test ran green against nonexistent path under `os.tmpdir()`)\n\nRESOLUTION: implemented\nCreated `src/test-branch-liveness-check.js` (node:test + node:assert/strict, CommonJS to match repo conventions) covering the real `src/branch-liveness-check.js` CLI: existing branch → exit 0/alive true, deleted branch → exit 1/alive false with reason \"gone\"/\"not on main\", missing repo path → exit 2/error true, and the single-JSON-object contract — each on a fresh `fs.mkdtempSync` temp git repo cleaned via `t.after()`. Ran `node src/test-branch-liveness-check.js`: 4/4 pass, exit code 0.\n\nAcceptance:\n1. File exists and imports node:test + node:assert/strict — grep lines 14-15 — PASS\n2. `node src/test-branch-liveness-check.js` exits 0 with ≥4 passing cases — run output `# pass 4, # fail 0, EXIT_CODE=0` — PASS\n3. Per-test temp repo via mkdtempSync + execFileSync('git'), rmSync cleanup — grep 5+6 hits — PASS\n4. Deleted-branch asserts status===1, alive===false, reason mentions gone/not on main — grep lines 75/79/82, test green — PASS\n5. Missing-repo asserts status===2, error===true — grep lines 92/95, test green — PASS",
          "rawDiffChars": 5355,
          "rawDiff": "diff --git a/src/test-branch-liveness-check.js b/src/test-branch-liveness-check.js\nnew file mode 100644\nindex 00000000..8872bbd3\n--- /dev/null\n+++ b/src/test-branch-liveness-check.js\n@@ -0,0 +1,114 @@\n+'use strict';\n+\n+// Tests for the branch-liveness-check.js guard CLI:\n+//   node src/branch-liveness-check.js --branch <name> --repo <path>\n+// stdout is one JSON object { branch, alive, reason } (plus error:true on the\n+// operational-error path); exit 0 = alive, 1 = dead, 2 = operational error.\n+//\n+// Every test builds a real throwaway git repo (fs.mkdtempSync + execFileSync,\n+// same fixture shape group-b-worktree-diff.test.js uses) and exercises the CLI\n+// as a child process via process.execPath, so the full CLI contract -- exit\n+// codes, JSON shape, and git behaviour -- is covered, not just the pure\n+// checkBranchLiveness() function.\n+\n+const test = require('node:test');\n+const assert = require('node:assert/strict');\n+const fs = require('node:fs');\n+const os = require('node:os');\n+const path = require('node:path');\n+const { execFileSync, spawnSync } = require('node:child_process');\n+\n+const CLI = path.join(__dirname, 'branch-liveness-check.js');\n+\n+/**\n+ * Fresh single-commit git repo on branch `main`. Each test gets its own dir so\n+ * no test depends on another test's repo or on any global git state; callers\n+ * must register cleanup via t.after().\n+ */\n+function createTempRepo() {\n+  const dir = fs.mkdtempSync(path.join(os.tmpdir(), 'branch-liveness-test-'));\n+  execFileSync('git', ['init', '-b', 'main'], { cwd: dir, stdio: 'pipe' });\n+  execFileSync('git', ['config', 'user.email', 'test@example.com'], { cwd: dir, stdio: 'pipe' });\n+  execFileSync('git', ['config', 'user.name', 'Liveness Test'], { cwd: dir, stdio: 'pipe' });\n+  fs.writeFileSync(path.join(dir, 'README.md'), 'hello\\n');\n+  execFileSync('git', ['add', '.'], { cwd: dir, stdio: 'pipe' });\n+  execFileSync('git', ['commit', '-m', 'initial commit'], { cwd: dir, stdio: 'pipe' });\n+  return dir;\n+}\n+\n+function runCli(branch, repoPath) {\n+  return spawnSync(process.execPath, [CLI, '--branch', branch, '--repo', repoPath], {\n+    encoding: 'utf8',\n+    timeout: 20_000,\n+  });\n+}\n+\n+test('existing branch -> exit 0, alive true', (t) => {\n+  const dir = createTempRepo();\n+  t.after(() => fs.rmSync(dir, { recursive: true, force: true }));\n+\n+  execFileSync('git', ['branch', 'feature-alive'], { cwd: dir, stdio: 'pipe' });\n+\n+  const res = runCli('feature-alive', dir);\n+  assert.equal(res.status, 0, `expected exit 0, got ${res.status}: ${res.stdout} ${res.stderr}`);\n+\n+  const out = JSON.parse(res.stdout);\n+  assert.equal(out.branch, 'feature-alive');\n+  assert.equal(out.alive, true);\n+  assert.equal(typeof out.reason, 'string');\n+});\n+\n+test('deleted branch -> exit 1, alive false, reason mentions gone/not on main', (t) => {\n+  const dir = createTempRepo();\n+  t.after(() => fs.rmSync(dir, { recursive: true, force: true }));\n+\n+  // NOTE: the branch name must not appear in any commit message -- the CLI's\n+  // tip-discovery falls back to `git log --all --grep=<branch>` after local and\n+  // origin refs are gone, and a grep hit that is an ancestor of main would flip\n+  // the result to \"work already on main\" (alive, exit 0). `feature-deleted-xyz`\n+  // does not match the only commit message (\"initial commit\"), so the branch is\n+  // genuinely undiscoverable -> dead.\n+  execFileSync('git', ['branch', 'feature-deleted-xyz'], { cwd: dir, stdio: 'pipe' });\n+  execFileSync('git', ['branch', '-D', 'feature-deleted-xyz'], { cwd: dir, stdio: 'pipe' });\n+\n+  const res = runCli('feature-deleted-xyz', dir);\n+  assert.equal(res.status, 1, `expected exit 1, got ${res.status}: ${res.stdout} ${res.stderr}`);\n+\n+  const out = JSON.parse(res.stdout);\n+  assert.equal(out.branch, 'feature-deleted-xyz');\n+  assert.equal(out.alive, false);\n+  const reasonLower = String(out.reason).toLowerCase();\n+  assert.ok(\n+    reasonLower.includes('gone') || reasonLower.includes('not on main'),\n+    `reason should mention \"gone\" or \"not on main\", got: \"${out.reason}\"`,\n+  );\n+});\n+\n+test('missing repo path -> exit 2, error true', (t) => {\n+  t.after(() => { /* nothing to clean up -- the point is that the path does not exist */ });\n+  const nonexistent = path.join(os.tmpdir(), 'definitely-not-a-repo-branch-liveness-test');\n+\n+  const res = runCli('some-branch', nonexistent);\n+  assert.equal(res.status, 2, `expected exit 2, got ${res.status}: ${res.stdout} ${res.stderr}`);\n+\n+  const out = JSON.parse(res.stdout);\n+  assert.equal(out.error, true);\n+});\n+\n+test('CLI output is a single JSON object with branch/alive/reason keys', (t) => {\n+  const dir = createTempRepo();\n+  t.after(() => fs.rmSync(dir, { recursive: true, force: true }));\n+\n+  const res = runCli('main', dir);\n+  assert.equal(res.status, 0, `expected exit 0, got ${res.status}: ${res.stdout} ${res.stderr}`);\n+\n+  const trimmed = res.stdout.trim();\n+  assert.ok(trimmed.startsWith('{'), `stdout should be a single JSON object, got: ${trimmed.slice(0, 80)}`);\n+\n+  const out = JSON.parse(trimmed);\n+  assert.equal(typeof out, 'object');\n+  assert.ok(!Array.isArray(out), 'stdout JSON must be an object, not an array');\n+  assert.ok('branch' in out, 'missing \"branch\" key');\n+  assert.ok('alive' in out, 'missing \"alive\" key');\n+  assert.ok('reason' in out, 'missing \"reason\" key');\n+});",
          "turnsUsed": 7,
          "toolCalls": {
            "total": 7,
            "byTool": {
              "run_bash": 3,
              "read_file": 2,
              "grep_codebase": 1,
              "write_file": 1
            },
            "errors": 0,
            "calls": [
              {
                "n": 1,
                "tool": "run_bash",
                "argKeys": [
                  "command"
                ],
                "bytes": 2369
              },
              {
                "n": 2,
                "tool": "read_file",
                "argKeys": [
                  "path",
                  "limit"
                ],
                "bytes": 2194
              },
              {
                "n": 3,
                "tool": "read_file",
                "argKeys": [
                  "path"
                ],
                "bytes": 7648
              },
              {
                "n": 4,
                "tool": "grep_codebase",
                "argKeys": [
                  "query",
                  "dir",
                  "contextLines"
                ],
                "bytes": 1115
              },
              {
                "n": 5,
                "tool": "write_file",
                "argKeys": [
                  "path",
                  "content"
                ],
                "bytes": 59
              },
              {
                "n": 6,
                "tool": "run_bash",
                "argKeys": [
                  "command"
                ],
                "bytes": 996
              },
              {
                "n": 7,
                "tool": "run_bash",
                "argKeys": [
                  "command"
                ],
                "bytes": 2253
              }
            ]
          }
        }
      ],
      "orient": {
        "skipped": false,
        "turnsUsed": 8
      },
      "plan": {
        "chars": 9812,
        "text": "## PLAN: Create `src/test-branch-liveness-check.js`\n\n### 0. Dependency check (do this first)\n\nThe test targets `src/branch-liveness-check.js`, which a sibling task (\"Create src/branch-liveness-check.js guard module\", hub status: *merged*) is supposed to have produced. However, a direct `read_file` attempt on that path returned **ENOENT** (see tool-error log above), and a repo-wide grep for `\"branch-liveness-check\"` in both `src/` and `.` returned **zero hits**. The hub's \"VERIFIED ON DISK\" line for that sibling says *\"not checked (could not parse a target file from this sibling's title)\"*, so it was never actually confirmed on disk.\n\n**Action:** Before writing a single line of test code, run:\n\n```bash\nls -la src/branch-liveness-check.js\n```\n\n- **If the file exists:** proceed to step 1.\n- **If it does not exist:** the test cannot pass (it will fail on the very first `spawnSync` with ENOENT). In that case, either (a) block this task and escalate to the hub that the sibling's \"merged\" status is incorrect, or (b) if the orchestrator explicitly instructs to proceed anyway, write the test so that it *skips* (via `test.skip` / `t.skip()`) when the target file is absent, but note that the task's own success criterion — *\"node src/test-branch-liveness-check.js must exit 0\"* — is only meaningfully met when the target exists. Flag this clearly in the commit message.\n\n> I am extrapolating the exact CLI argument contract (positional `repoPath branchName` vs. flags) because the target file's content is not available. The raw-text prompt specifies the *output* contract (JSON with `branch`/`alive`/`reason`, exit codes 0/1/2) but not the *input* contract. **Verify the actual CLI signature once the file is confirmed present** (read the first ~40 lines of `src/branch-liveness-check.js`) before finalising the `args` arrays in the test.\n\n---\n\n### 1. Create the file `src/test-branch-liveness-check.js`\n\nScaffold the module using the same conventions visible in the existing test files in this repo (`src/agentic-draft-common.test.js`, `scripts/gpu-arbiter-cli.test.js` — both confirmed to exist). The repo already uses `child_process` in 20 places under `src/` and references `process.execPath` (1 hit in `src/`, 2 in the repo root), so spawning a sibling script via `process.execPath` is an established pattern here.\n\n```js\n'use strict';\n\nconst { test } = require('node:test');\nconst assert = require('node:assert/strict');\nconst fs = require('node:fs');\nconst path = require('node:path');\nconst os = require('node:os');\nconst { execFileSync, spawnSync } = require('node:child_process');\n```\n\n> I am extrapolating that the repo uses CommonJS (`require`) based on the presence of `node:test` and `node:assert/strict` in the raw-text spec and the `child_process` usage pattern. **Verify** by checking the first few lines of `src/agentic-draft-common.test.js` for `require` vs `import`. If it is ESM, switch to `import` syntax.\n\n---\n\n### 2. Write a `createTempRepo()` helper\n\nThis helper builds a throwaway git repository for each test case:\n\n```js\nfunction createTempRepo() {\n  const dir = fs.mkdtempSync(path.join(os.tmpdir(), 'liveness-test-'));\n  execFileSync('git', ['init', '-b', 'main'], { cwd: dir, stdio: 'pipe' });\n  execFileSync('git', ['config', 'user.email', 'test@example.com'], { cwd: dir, stdio: 'pipe' });\n  execFileSync('git', ['config', 'user.name', 'Test'], { cwd: dir, stdio: 'pipe' });\n  fs.writeFileSync(path.join(dir, 'README.md'), 'hello\\n');\n  execFileSync('git', ['add', '.'], { cwd: dir, stdio: 'pipe' });\n  execFileSync('git', ['commit', '-m', 'initial commit'], { cwd: dir, stdio: 'pipe' });\n  return dir;\n}\n```\n\n- `fs.mkdtempSync` is explicitly called out in the task spec.\n- `execFileSync` for the git commands is also specified.\n- Each test gets a **fresh** temp dir (call `createTempRepo()` inside the test body, not at module scope) so tests are independent.\n- Clean up with `fs.rmSync(dir, { recursive: true, force: true })` in a `t.after()` or `try/finally` per test.\n\n---\n\n### 3. Write a `runCli(repoPath, branchName)` helper\n\nSpawns the target CLI and returns the `spawnSync` result:\n\n```js\nconst CLI_PATH = path.join(__dirname, 'branch-liveness-check.js');\n\nfunction runCli(repoPath, branchName) {\n  return spawnSync(process.execPath, [CLI_PATH, repoPath, branchName], {\n    encoding: 'utf8',\n    timeout: 10_000,\n  });\n}\n```\n\n> **Extrapolation:** I am assuming the CLI takes two positional args (`repoPath`, `branchName`). The raw-text spec says \"missing repo path → exit 2\", implying the first positional arg is the repo path. **Verify the actual argument order and whether flags are used** by reading `src/branch-liveness-check.js` once it is confirmed present. Adjust the `args` array accordingly.\n\nUsing `process.execPath` (rather than the literal string `'node'`) matches the pattern already found in the repo (1 hit in `src/`, 2 in `.`) and avoids PATH issues in CI.\n\n---\n\n### 4. Test case: existing branch → exit 0, alive true\n\n```js\ntest('existing branch → exit 0, alive true', (t) => {\n  const dir = createTempRepo();\n  t.after(() => fs.rmSync(dir, { recursive: true, force: true }));\n\n  // Create a feature branch\n  execFileSync('git', ['branch', 'feature-x'], { cwd: dir, stdio: 'pipe' });\n\n  const res = runCli(dir, 'feature-x');\n\n  assert.equal(res.status, 0, `expected exit 0, got ${res.status}: ${res.stderr}`);\n\n  const out = JSON.parse(res.stdout);\n  assert.equal(out.branch, 'feature-x');\n  assert.equal(out.alive, true);\n  assert.equal(typeof out.reason, 'string');\n});\n```\n\n---\n\n### 5. Test case: deleted branch → exit 1, alive false, reason mentions \"gone\" or \"not on main\"\n\n```js\ntest('deleted branch → exit 1, alive false, reason mentions gone/not on main', (t) => {\n  const dir = createTempRepo();\n  t.after(() => fs.rmSync(dir, { recursive: true, force: true }));\n\n  // Create then delete the branch\n  execFileSync('git', ['branch', 'feature-y'], { cwd: dir, stdio: 'pipe' });\n  execFileSync('git', ['branch', '-D', 'feature-\n... [truncated 3812 more chars]",
        "attempts": 3,
        "grounded": true,
        "groundingChars": 2694
      },
      "outcome": "succeeded",
      "adhocResolution": "implemented",
      "acceptance": {
        "criteriaCount": 5,
        "resultCount": 5,
        "passCount": 0,
        "source": "plan-derived"
      },
      "finishedAt": "2026-09-13T01:52:47.563Z"
    }
  ],
  "localVerdict": "Confident majority APPROVE (votes: 2/3 real)\n\nAPPROVE: The grounding source confirms `src/branch-liveness-check.js` exists (7342 bytes) and the test run output shows all 4 tests passing with `EXIT_CODE=0`, satisfying every acceptance criterion including the specific assertions for deleted-branch (status 1, alive false, reason \"gone\"/\"not on main\") and missing-repo (status 2, error true).",
  "localVotes": [
    {
      "verdict": "APPROVE",
      "response": "APPROVE: The grounding source confirms `src/branch-liveness-check.js` exists (7342 bytes) and the test run output shows all 4 tests passing with `EXIT_CODE=0`, satisfying every acceptance criterion including the specific assertions for deleted-branch (status 1, alive false, reason \"gone\"/\"not on main\") and missing-repo (status 2, error true)."
    },
    {
      "verdict": "APPROVE",
      "response": "APPROVE: The grounding source confirms `src/branch-liveness-check.js` exists (7342 bytes, dated Sep 13), the test file was written (`\"written\":true`), and the real `node src/test-branch-liveness-check.js` run returned `EXIT_CODE=0` with `# pass 4 / # fail 0` covering all four required cases (existing-branch, deleted-branch, missing-repo, JSON-contract); the diff's assertions match criteria 4 (`res.status, 1`, `out.alive, false`, reason includes \"gone\"/\"not on main\") and 5 (`res.status, 2`, `out.error, true`), and each test builds its own temp repo via `fs.mkdtempSync` + `execFileSync('git', …)` with `t.after(() => fs.rmSync(...))` cleanup."
    }
  ],
  "voteErrors": [],
  "acceptanceCriteria": [
    "`src/test-branch-liveness-check.js` exists on disk and is a valid Node.js module that imports `node:test` and `node:assert/strict`.",
    "Running `node src/test-branch-liveness-check.js` in the repo root exits with code **0** and prints at least 4 passing test cases (existing-branch, deleted-branch, missing-repo, JSON-contract).",
    "Each test case creates its own temp git repo via `fs.mkdtempSync` + `execFileSync('git', …)` and cleans it up; no test depends on a pre-existing repo or global git state.",
    "The deleted-branch test asserts `res.status === 1`, `out.alive === false`, and that `out.reason` (case-insensitive) contains the substring `\"gone\"` or `\"not on main\"`.",
    "The missing-repo test asserts `res.status === 2` and `out.error === true`."
  ],
  "draftModel": "qwen3.8:27b-q4_K_M",
  "implementResponse": "All checks pass. The `git add`/commit failed only because the sandbox's `.git/objects` directory is on a read-only filesystem (env limitation, exit 128 \"Read-only file system\") — the file itself is written on disk and all acceptance-criteria checks that matter (file exists, tests run, exit 0) pass. The commit is normally handled by the pipeline harness.\n\nSummary: I created `src/test-branch-liveness-check.js` targeting the real, on-disk `src/branch-liveness-check.js` (the sibling's \"merged\" file was genuinely present — the prior investigation's ENOENT was stale). Two corrections to the pre-plan: the CLI takes **flags** `--branch <name> --repo <path>`, not positional args; and the deleted-branch test had to use a branch name absent from all commit messages, because the CLI's tip-discovery falls back to `git log --all --grep=<branch>`, which would otherwise resurrect the deleted branch as \"work already on main\" and flip it to alive.\n\nAcceptance:\n1. `src/test-branch-liveness-check.js` exists and imports `node:test` + `node:assert/strict` — `grep -n \"node:test|node:assert/strict\"` — PASS (lines 14-15: `require('node:test')`, `require('node:assert/strict')`)\n2. `node src/test-branch-liveness-check.js` exits 0 with ≥4 passing tests — ran it — PASS (`# pass 4 / # fail 0`, `EXIT_CODE=0`, all 4 named cases ok)\n3. Each test creates its own temp git repo via `fs.mkdtempSync` + `execFileSync('git', …)` and cleans up — `grep -cn \"mkdtempSync|rmSync\"` = 5, plus 6 `execFileSync('git'` sites — PASS (per-test `createTempRepo()` + `t.after(() => fs.rmSync(dir, {recursive:true, force:true}))`)\n4. Deleted-branch test asserts `res.status === 1`, `out.alive === false`, reason mentions \"gone\"/\"not on main\" — `grep -n` lines 75/79/82 — PASS (test ran green; actual CLI reason \"branch gone, not on main: work would be lost\" satisfies both substrings)\n5. Missing-repo test asserts `res.status === 2` and `out.error === true` — `grep -n` lines 92/95 — PASS (test ran green against nonexistent path under `os.tmpdir()`)\n\nRESOLUTION: implemented\nCreated `src/test-branch-liveness-check.js` (node:test + node:assert/strict, CommonJS to match repo conventions) covering the real `src/branch-liveness-check.js` CLI: existing branch → exit 0/alive true, deleted branch → exit 1/alive false with reason \"gone\"/\"not on main\", missing repo path → exit 2/error true, and the single-JSON-object contract — each on a fresh `fs.mkdtempSync` temp git repo cleaned via `t.after()`. Ran `node src/test-branch-liveness-check.js`: 4/4 pass, exit code 0.\n\nAcceptance:\n1. File exists and imports node:test + node:assert/strict — grep lines 14-15 — PASS\n2. `node src/test-branch-liveness-check.js` exits 0 with ≥4 passing cases — run output `# pass 4, # fail 0, EXIT_CODE=0` — PASS\n3. Per-test temp repo via mkdtempSync + execFileSync('git'), rmSync cleanup — grep 5+6 hits — PASS\n4. Deleted-branch asserts status===1, alive===false, reason mentions gone/not on main — grep lines 75/79/82, test green — PASS\n5. Missing-repo asserts status===2, error===true — grep lines 92/95, test green — PASS\n\n=== DIFF ===\ndiff --git a/src/test-branch-liveness-check.js b/src/test-branch-liveness-check.js\nnew file mode 100644\nindex 00000000..8872bbd3\n--- /dev/null\n+++ b/src/test-branch-liveness-check.js\n@@ -0,0 +1,114 @@\n+'use strict';\n+\n+// Tests for the branch-liveness-check.js guard CLI:\n+//   node src/branch-liveness-check.js --branch <name> --repo <path>\n+// stdout is one JSON object { branch, alive, reason } (plus error:true on the\n+// operational-error path); exit 0 = alive, 1 = dead, 2 = operational error.\n+//\n+// Every test builds a real throwaway git repo (fs.mkdtempSync + execFileSync,\n+// same fixture shape group-b-worktree-diff.test.js uses) and exercises the CLI\n+// as a child process via process.execPath, so the full CLI contract -- exit\n+// codes, JSON shape, and git behaviour -- is covered, not just the pure\n+// checkBranchLiveness() function.\n+\n+const test = require('node:test');\n+const assert = require('node:assert/strict');\n+const fs = require('node:fs');\n+const os = require('node:os');\n+const path = require('node:path');\n+const { execFileSync, spawnSync } = require('node:child_process');\n+\n+const CLI = path.join(__dirname, 'branch-liveness-check.js');\n+\n+/**\n+ * Fresh single-commit git repo on branch `main`. Each test gets its own dir so\n+ * no test depends on another test's repo or on any global git state; callers\n+ * must register cleanup via t.after().\n+ */\n+function createTempRepo() {\n+  const dir = fs.mkdtempSync(path.join(os.tmpdir(), 'branch-liveness-test-'));\n+  execFileSync('git', ['init', '-b', 'main'], { cwd: dir, stdio: 'pipe' });\n+  execFileSync('git', ['config', 'user.email', 'test@example.com'], { cwd: dir, stdio: 'pipe' });\n+  execFileSync('git', ['config', 'user.name', 'Liveness Test'], { cwd: dir, stdio: 'pipe' });\n+  fs.writeFileSync(path.join(dir, 'README.md'), 'hello\\n');\n+  execFileSync('git', ['add', '.'], { cwd: dir, stdio: 'pipe' });\n+  execFileSync('git', ['commit', '-m', 'initial commit'], { cwd: dir, stdio: 'pipe' });\n+  return dir;\n+}\n+\n+function runCli(branch, repoPath) {\n+  return spawnSync(process.execPath, [CLI, '--branch', branch, '--repo', repoPath], {\n+    encoding: 'utf8',\n+    timeout: 20_000,\n+  });\n+}\n+\n+test('existing branch -> exit 0, alive true', (t) => {\n+  const dir = createTempRepo();\n+  t.after(() => fs.rmSync(dir, { recursive: true, force: true }));\n+\n+  execFileSync('git', ['branch', 'feature-alive'], { cwd: dir, stdio: 'pipe' });\n+\n+  const res = runCli('feature-alive', dir);\n+  assert.equal(res.status, 0, `expected exit 0, got ${res.status}: ${res.stdout} ${res.stderr}`);\n+\n+  const out = JSON.parse(res.stdout);\n+  assert.equal(out.branch, 'feature-alive');\n+  assert.equal(out.alive, true);\n+  assert.equal(typeof out.reason, 'string');\n+});\n+\n+test('deleted branch -> exit 1, alive false, reason mentions gone/not on main', (t) => {\n+  const dir = createTempRepo();\n+  t.after(() => fs.rmSync(dir, { recursive: true, force: true }));\n+\n+  // NOTE: the branch name must not appear in any commit message -- the CLI's\n+  // tip-discovery falls back to `git log --all --grep=<branch>` after local and\n+  // origin refs are gone, and a grep hit that is an ancestor of main would flip\n+  // the result to \"work already on main\" (alive, exit 0). `feature-deleted-xyz`\n+  // does not match the only commit message (\"initial commit\"), so the branch is\n+  // genuinely undiscoverable -> dead.\n+  execFileSync('git', ['branch', 'feature-deleted-xyz'], { cwd: dir, stdio: 'pipe' });\n+  execFileSync('git', ['branch', '-D', 'feature-deleted-xyz'], { cwd: dir, stdio: 'pipe' });\n+\n+  const res = runCli('feature-deleted-xyz', dir);\n+  assert.equal(res.status, 1, `expected exit 1, got ${res.status}: ${res.stdout} ${res.stderr}`);\n+\n+  const out = JSON.parse(res.stdout);\n+  assert.equal(out.branch, 'feature-deleted-xyz');\n+  assert.equal(out.alive, false);\n+  const reasonLower = String(out.reason).toLowerCase();\n+  assert.ok(\n+    reasonLower.includes('gone') || reasonLower.includes('not on main'),\n+    `reason should mention \"gone\" or \"not on main\", got: \"${out.reason}\"`,\n+  );\n+});\n+\n+test('missing repo path -> exit 2, error true', (t) => {\n+  t.after(() => { /* nothing to clean up -- the point is that the path does not exist */ });\n+  const nonexistent = path.join(os.tmpdir(), 'definitely-not-a-repo-branch-liveness-test');\n+\n+  const res = runCli('some-branch', nonexistent);\n+  assert.equal(res.status, 2, `expected exit 2, got ${res.status}: ${res.stdout} ${res.stderr}`);\n+\n+  const out = JSON.parse(res.stdout);\n+  assert.equal(out.error, true);\n+});\n+\n+test('CLI output is a single JSON object with branch/alive/reason keys', (t) => {\n+  const dir = createTempRepo();\n+  t.after(() => fs.rmSync(dir, { recursive: true, force: true }));\n+\n+  const res = runCli('main', dir);\n+  assert.equal(res.status, 0, `expected exit 0, got ${res.status}: ${res.stdout} ${res.stderr}`);\n+\n+  const trimmed = res.stdout.trim();\n+  assert.ok(trimmed.startsWith('{'), `stdout should be a single JSON object, got: ${trimmed.slice(0, 80)}`);\n+\n+  const out = JSON.parse(trimmed);\n+  assert.equal(typeof out, 'object');\n+  assert.ok(!Array.isArray(out), 'stdout JSON must be an object, not an array');\n+  assert.ok('branch' in out, 'missing \"branch\" key');\n+  assert.ok('alive' in out, 'missing \"alive\" key');\n+  assert.ok('reason' in out, 'missing \"reason\" key');\n+});",
  "planResponse": "## PLAN: Create `src/test-branch-liveness-check.js`\n\n### 0. Dependency check (do this first)\n\nThe test targets `src/branch-liveness-check.js`, which a sibling task (\"Create src/branch-liveness-check.js guard module\", hub status: *merged*) is supposed to have produced. However, a direct `read_file` attempt on that path returned **ENOENT** (see tool-error log above), and a repo-wide grep for `\"branch-liveness-check\"` in both `src/` and `.` returned **zero hits**. The hub's \"VERIFIED ON DISK\" line for that sibling says *\"not checked (could not parse a target file from this sibling's title)\"*, so it was never actually confirmed on disk.\n\n**Action:** Before writing a single line of test code, run:\n\n```bash\nls -la src/branch-liveness-check.js\n```\n\n- **If the file exists:** proceed to step 1.\n- **If it does not exist:** the test cannot pass (it will fail on the very first `spawnSync` with ENOENT). In that case, either (a) block this task and escalate to the hub that the sibling's \"merged\" status is incorrect, or (b) if the orchestrator explicitly instructs to proceed anyway, write the test so that it *skips* (via `test.skip` / `t.skip()`) when the target file is absent, but note that the task's own success criterion — *\"node src/test-branch-liveness-check.js must exit 0\"* — is only meaningfully met when the target exists. Flag this clearly in the commit message.\n\n> I am extrapolating the exact CLI argument contract (positional `repoPath branchName` vs. flags) because the target file's content is not available. The raw-text prompt specifies the *output* contract (JSON with `branch`/`alive`/`reason`, exit codes 0/1/2) but not the *input* contract. **Verify the actual CLI signature once the file is confirmed present** (read the first ~40 lines of `src/branch-liveness-check.js`) before finalising the `args` arrays in the test.\n\n---\n\n### 1. Create the file `src/test-branch-liveness-check.js`\n\nScaffold the module using the same conventions visible in the existing test files in this repo (`src/agentic-draft-common.test.js`, `scripts/gpu-arbiter-cli.test.js` — both confirmed to exist). The repo already uses `child_process` in 20 places under `src/` and references `process.execPath` (1 hit in `src/`, 2 in the repo root), so spawning a sibling script via `process.execPath` is an established pattern here.\n\n```js\n'use strict';\n\nconst { test } = require('node:test');\nconst assert = require('node:assert/strict');\nconst fs = require('node:fs');\nconst path = require('node:path');\nconst os = require('node:os');\nconst { execFileSync, spawnSync } = require('node:child_process');\n```\n\n> I am extrapolating that the repo uses CommonJS (`require`) based on the presence of `node:test` and `node:assert/strict` in the raw-text spec and the `child_process` usage pattern. **Verify** by checking the first few lines of `src/agentic-draft-common.test.js` for `require` vs `import`. If it is ESM, switch to `import` syntax.\n\n---\n\n### 2. Write a `createTempRepo()` helper\n\nThis helper builds a throwaway git repository for each test case:\n\n```js\nfunction createTempRepo() {\n  const dir = fs.mkdtempSync(path.join(os.tmpdir(), 'liveness-test-'));\n  execFileSync('git', ['init', '-b', 'main'], { cwd: dir, stdio: 'pipe' });\n  execFileSync('git', ['config', 'user.email', 'test@example.com'], { cwd: dir, stdio: 'pipe' });\n  execFileSync('git', ['config', 'user.name', 'Test'], { cwd: dir, stdio: 'pipe' });\n  fs.writeFileSync(path.join(dir, 'README.md'), 'hello\\n');\n  execFileSync('git', ['add', '.'], { cwd: dir, stdio: 'pipe' });\n  execFileSync('git', ['commit', '-m', 'initial commit'], { cwd: dir, stdio: 'pipe' });\n  return dir;\n}\n```\n\n- `fs.mkdtempSync` is explicitly called out in the task spec.\n- `execFileSync` for the git commands is also specified.\n- Each test gets a **fresh** temp dir (call `createTempRepo()` inside the test body, not at module scope) so tests are independent.\n- Clean up with `fs.rmSync(dir, { recursive: true, force: true })` in a `t.after()` or `try/finally` per test.\n\n---\n\n### 3. Write a `runCli(repoPath, branchName)` helper\n\nSpawns the target CLI and returns the `spawnSync` result:\n\n```js\nconst CLI_PATH = path.join(__dirname, 'branch-liveness-check.js');\n\nfunction runCli(repoPath, branchName) {\n  return spawnSync(process.execPath, [CLI_PATH, repoPath, branchName], {\n    encoding: 'utf8',\n    timeout: 10_000,\n  });\n}\n```\n\n> **Extrapolation:** I am assuming the CLI takes two positional args (`repoPath`, `branchName`). The raw-text spec says \"missing repo path → exit 2\", implying the first positional arg is the repo path. **Verify the actual argument order and whether flags are used** by reading `src/branch-liveness-check.js` once it is confirmed present. Adjust the `args` array accordingly.\n\nUsing `process.execPath` (rather than the literal string `'node'`) matches the pattern already found in the repo (1 hit in `src/`, 2 in `.`) and avoids PATH issues in CI.\n\n---\n\n### 4. Test case: existing branch → exit 0, alive true\n\n```js\ntest('existing branch → exit 0, alive true', (t) => {\n  const dir = createTempRepo();\n  t.after(() => fs.rmSync(dir, { recursive: true, force: true }));\n\n  // Create a feature branch\n  execFileSync('git', ['branch', 'feature-x'], { cwd: dir, stdio: 'pipe' });\n\n  const res = runCli(dir, 'feature-x');\n\n  assert.equal(res.status, 0, `expected exit 0, got ${res.status}: ${res.stderr}`);\n\n  const out = JSON.parse(res.stdout);\n  assert.equal(out.branch, 'feature-x');\n  assert.equal(out.alive, true);\n  assert.equal(typeof out.reason, 'string');\n});\n```\n\n---\n\n### 5. Test case: deleted branch → exit 1, alive false, reason mentions \"gone\" or \"not on main\"\n\n```js\ntest('deleted branch → exit 1, alive false, reason mentions gone/not on main', (t) => {\n  const dir = createTempRepo();\n  t.after(() => fs.rmSync(dir, { recursive: true, force: true }));\n\n  // Create then delete the branch\n  execFileSync('git', ['branch', 'feature-y'], { cwd: dir, stdio: 'pipe' });\n  execFileSync('git', ['branch', '-D', 'feature-y'], { cwd: dir, stdio: 'pipe' });\n\n  const res = runCli(dir, 'feature-y');\n\n  assert.equal(res.status, 1, `expected exit 1, got ${res.status}: ${res.stderr}`);\n\n  const out = JSON.parse(res.stdout);\n  assert.equal(out.branch, 'feature-y');\n  assert.equal(out.alive, false);\n  const reasonLower = out.reason.toLowerCase();\n  assert.ok(\n    reasonLower.includes('gone') || reasonLower.includes('not on main'),\n    `reason should mention \"gone\" or \"not on main\", got: \"${out.reason}\"`,\n  );\n});\n```\n\n> The raw-text spec says the reason should mention \"gone\" **or** \"not on main\". I am using a case-insensitive `includes` check to be tolerant of exact wording. **Verify** the actual reason string the CLI emits once the target file is available, and tighten the assertion if needed.\n\n---\n\n### 6. Test case: missing repo path → exit 2, error true\n\n```js\ntest('missing repo path → exit 2, error true', (t) => {\n  const nonexistent = path.join(os.tmpdir(), 'definitely-not-a-repo-xyz');\n\n  const res = runCli(nonexistent, 'some-branch');\n\n  assert.equal(res.status, 2, `expected exit 2, got ${res.status}: ${res.stderr}`);\n\n  const out = JSON.parse(res.stdout);\n  assert.equal(out.error, true);\n});\n```\n\n> **Extrapolation:** The raw-text spec says \"exit 2 with error true\" but does not specify whether `error` is a top-level key or nested. I am assuming it is a top-level boolean key in the same JSON object. **Verify** against the actual CLI output.\n\n---\n\n### 7. Test case: CLI contract — single JSON object with `branch`/`alive`/`reason` keys\n\nThis can be folded into the assertions above (each test already parses `res.stdout` as JSON and checks for the three keys), but add one explicit structural test to make the contract visible:\n\n```js\ntest('CLI output is a single JSON object with branch/alive/reason keys', (t) => {\n  const dir = createTempRepo();\n  t.after(() => fs.rmSync(dir, { recursive: true, force: true }));\n\n  const res = runCli(dir, 'main');\n\n  assert.equal(res.status, 0);\n\n  // Must be valid JSON (single object, not an array, not multiple lines)\n  const trimmed = res.stdout.trim();\n  assert.ok(trimmed.startsWith('{'), `stdout should be a JSON object, got: ${trimmed.slice(0, 80)}`);\n\n  const out = JSON.parse(trimmed);\n  assert.equal(typeof out, 'object');\n  assert.ok(!Array.isArray(out));\n  assert.ok('branch' in out, 'missing \"branch\" key');\n  assert.ok('alive' in out, 'missing \"alive\" key');\n  assert.ok('reason' in out, 'missing \"reason\" key');\n});\n```\n\n---\n\n### 8. Run the test and confirm exit 0\n\n```bash\nnode src/test-branch-liveness-check.js\necho \"exit code: $?\"\n```\n\n- All four (or five) test cases must pass.\n- The process must exit with code **0**.\n- If any test fails, read the assertion message, fix the test (or, if the CLI behaviour is genuinely different from the spec, flag it as a bug in the sibling task's output rather than silently adjusting the test).\n\n---\n\n### 9. Commit\n\n```bash\ngit add src/test-branch-liveness-check.js\ngit commit -m \"test: add liveness-check CLI tests (node:test + temp git repo)\"\n```\n\n---\n\nCRITERIA:\n- `src/test-branch-liveness-check.js` exists on disk and is a valid Node.js module that imports `node:test` and `node:assert/strict`.\n- Running `node src/test-branch-liveness-check.js` in the repo root exits with code **0** and prints at least 4 passing test cases (existing-branch, deleted-branch, missing-repo, JSON-contract).\n- Each test case creates its own temp git repo via `fs.mkdtempSync` + `execFileSync('git', …)` and cleans it up; no test depends on a pre-existing repo or global git state.\n- The deleted-branch test asserts `res.status === 1`, `out.alive === false`, and that `out.reason` (case-insensitive) contains the substring `\"gone\"` or `\"not on main\"`.\n- The missing-repo test asserts `res.status === 2` and `out.error === true`.",
  "reviewedAt": "2026-09-13T01:53:50.285Z",
  "claimedAt": "2026-09-13T01:40:38.684Z"
}
